export enum StatusPayment
{
    PENDING = "PENDING",
    COMPLETED = "COMPLETED",
    FAILED = "FAILED"
}

export enum TypePayment{
    DEPOSIT = "DEPOSIT",
    WITHDRAW = "WITHDRAW",
    ODER_PAYMENT = "ORDER_PAYMENT"
}
export enum Role{
    ADMIN = 1,
    USER = 2,
}
